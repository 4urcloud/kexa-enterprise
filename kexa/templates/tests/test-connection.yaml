apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kexa.fullname" . }}-test-connection"
  labels:
    {{- include "kexa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kexa.fullname" . }}:{{ .Values.servicefront.port }}']
  restartPolicy: Never
